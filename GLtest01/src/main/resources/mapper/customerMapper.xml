<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="customer">
	<!-- 고객 sql -->
	<select id="selectOne" parameterType="String" resultType="com.gl.master.model.customer.CustomerVo">
		select * from custest where id=#{value}
	</select>
</mapper>
